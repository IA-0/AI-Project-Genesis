# Copiar a .env (ya ignorado por git) y completar con la key real.
# Ver CLAUDE.md §Credenciales — nunca commitear .env con el valor real.

# Solo el rol Arquitecto la usa (ADR-0004). Sin esta key, Atlas corre en modo degradado
# (todo Groq) y lo marca en el frontmatter de 30_arquitectura.md.
ANTHROPIC_API_KEY=
